<%= form_with(model: city, local: true) do |form| %>
  <% if city.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(city.errors.count, "error") %> prohibited this city from being saved:</h2>

      <ul>
      <% city.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="field">
    <%= form.label :code %>
    <%= form.number_field :code %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= form.text_field :image %>
  </div>

  <div class="field">
    <%= form.label :state %>
    <%#= form.select("state_id", State.all.collect {|s| [s.description, s.id]}, {include_blank: 'Selecione...'}) %>
    <%#= form.select("state_id", State.all.pluck {:description, s.id]}, {include_blank: 'Selecione...'}) %>
    <%= form.select("state_id", @state_options, {include_blank: 'Selecione...'}) %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
